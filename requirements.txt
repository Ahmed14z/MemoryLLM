# Core dependencies (uses system torch)
cachetools
einops
ipdb
nltk
omegaconf
packaging
PyYAML
tqdm
SentencePiece
numpy

# HuggingFace
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.29.3
safetensors
datasets

# For 4-bit quantization on smaller GPUs
bitsandbytes

# Optional: only needed for training
# pytorch_lightning==2.2.2
# deepspeed==0.14.1
# tensorboard
